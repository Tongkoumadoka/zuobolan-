## 生命周期

````javascript
//vue实例挂载完毕
mounted(){
	console.log('vm开始管理容器')	
    // 也就是一显示页面就有如下功能
    // 1. 计时器
    setInterval((// 代码体 )=>{        
    },2000)
}

this.$destroy() 调用销毁钩子
// vm孩子,但是它不再管理挂载的容器数据了,它只是移除所有状态的监听,同时也不再更新数据了,并没有实际真的销毁

//就像身体脱离了大脑的控制
//真要彻底移除计时器,就在beforedestroy()中移除
````



